[Unit]
Description=Process due subscription cancellations nightly (02:00 UTC)

[Timer]
OnCalendar=*-*-* 02:00:00
# Catch up after downtime so a cancellation never slips past its
# effective date unprocessed.
Persistent=true

[Install]
WantedBy=timers.target
